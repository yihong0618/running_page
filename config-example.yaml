# Copy this file to `config.yaml` and set the configuration items

sync:
  garmin:
    authentication_domain: Global #  Global (default) | CN (Mainland China)
    email: user@email.com
    password: yourpassword
